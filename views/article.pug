extends layout

block content
    h1 #{title}
    p #{body}
    p - #{author}
    hr
    if user
        a.btn.btn-default(href='/article/edit/' + id) Edit
        a(href=`/article/${id}`)
        a.btn.btn-danger.delete-article(href="#", data-id=id) Delete